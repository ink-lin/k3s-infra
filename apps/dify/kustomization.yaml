apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dify

resources:
  - base-rendered.yaml  # 確保 "-" 後面有一個空白鍵 [cite: 1]
  - secrets/redis-creds.sealed.yaml
  - secrets/postgres-creds.sealed.yaml

patches:
  - path: patches/pvc-fix.yaml
  - path: patches/service-exposure.yaml # 如果這也是獨立檔案且內含 metadata，直接引用即可
